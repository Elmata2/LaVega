# De drie overgebleven voorstellen, samengevoegd

Bronnen: `docs/catalog/staging-kaartkosten.json` (22 augustus),
`docs/catalog/staging-account-fees.json` (21 augustus) en
`docs/superpowers/specs/2026-08-20-catalog-fx-gaps-and-ing-punten-data.md`.
Doelbestanden: `docs/catalog/catalog.json` en `docs/catalog/state.json`.
De puntenstaging is niet aangeraakt; die was op 23–24 augustus al verwerkt.

De diff is **371 toegevoegde regels en 6 verwijderde**. Van die zes zijn er vier
een `"fields": {}` dat vol is gelopen; de andere twee zijn de twee datums uit
§1b van de spec. Er is geen enkele waarde overschreven.

## De toelatingseis

Waarde + bron + datum + voorwaarden, alle vier, en de datum is die van het
**document**. Dat is dezelfde poort als `isCovered` in
`packages/core/src/catalog.ts`; `readAccountFee` weigert daarnaast een bedrag
zonder eenheid. Elke rij hieronder is er langs geweest, en wat er niet doorheen
kwam staat verderop met de reden.

`accountFee` draagt `period` naast de waarde. **Er is nergens omgerekend.** Amex
noemt de Platinum per maand en ICS zijn kaarten per jaar; het verschil is een
factor twaalf, en het document bepaalt de eenheid.

---

## Wat erin ging

### 1. Vijf kaartprijzen uit de kostensweep

| rij | bedrag | eenheid | documentdatum | bron |
|---|---|---|---|---|
| `bleap-card` | € 0,00 | per maand | 2026-06-28 | Cardholder Terms EEA, artikel 6.1 |
| `wirex-card-wirex-one` | € 0,00 | per maand | 2024-01-11 | X-tras pricing plans and tiers |
| `bybit-card` | € 0,00 | per **jaar** | 2026-06-03 | Fees and Spending Limits |
| `crypto-com-prepaid-card-basic-midnight-blue` | € 0,00 | per maand | 2026-04-22 | How to apply (helpcentrum) |
| `nexo-card` | € 0,00 | per maand | 2026-07-31 | nexo.com/crypto-card |

Vijf uitgesproken nullen, en dat is iets anders dan een ontbrekend cijfer. Ze
dragen alle vijf in hun voorwaarden mee wat er **wel** is: bij Bleap de
Mastercard scheme fees en netwerkkoersen, bij Wirex de bezorging (5 of 15 EUR)
en de tiers die wél geld kosten, bij Bybit de 2% geldopname en de tijdelijk
opgeschorte uitgifte, bij Crypto.com Basic de EUR 4,99 kaart en de EUR 5 per
maand slaapkosten, bij Nexo de tierdrempels voor een fysieke kaart.

Drie dingen zijn met de hand gezet in plaats van klakkeloos overgenomen:

- **Bybit staat op `jaar`.** De tabelrij heet "Annual Fee". De vier andere staan
  op `maand`. Wie hier één eenheid van maakt om de lijst netjes te krijgen,
  verandert een prijs met een factor twaalf.
- **De eenheid van Wirex is een leesbeslissing en dat staat er nu bij.** Het
  document hangt geen periode aan het woord "free"; `maand` komt uit de
  buurplannen in dezelfde tabelrij (EUR 9,99 en EUR 29,99 per maand). Bij een nul
  maakt het niets uit voor de som, maar het is geen citaat, en dan hoort het in
  de voorwaarden te staan.
- **Bij Wirex staat de houdbaarheid erbij.** Twee en een half jaar oud, het
  tarievenartikel waarschuwt zelf dat de tarieven kunnen wijzigen, en Wirex heeft
  zijn EEA-uitgifte sindsdien verbouwd. Het cijfer komt door de eis; het is nog
  steeds het zwakste in dit veld.

### 2. De weigering die in de sweep zat, is overgenomen

**De vier Crypto.com-niveaus met 2, 3, 4 en 5% cashback hebben GEEN
`accountFee` gekregen.** Hun nul is echt en gedateerd, maar hij geldt de KAART
terwijl de cashback aan een Level Up-niveau hangt dat geld kost of een CRO-inleg
vraagt — en dat bedrag heeft geen gedateerde bron (het staat alleen op
`crypto.com/nl/cards`, en die pagina draagt geen enkele datum).

De staging stelde voor ze als `{ kind: "unknown", reason: "needs-another-product" }`
te laten landen. Dat is in de catalogus alleen te bereiken door **wél een nul in
te vullen** en dan te vertrouwen op een regex: `isPricedOnItsOwn` in
`accountCosts.ts` zet `pricedOnItsOwn` op false zodra de voorwaardentekst
"alleen binnen een …" bevat, en `leesVoorwaarden` in de extensie zet er een
`voorwaardelijke-nul` op zodra de tekst niet "uitgesproken nul" zegt. Beide
mechanismen zijn nagemeten en ze werken.

**Ze zijn toch niet gebruikt, en dit is de afweging.** Obsidian draagt met 5% het
hoogste cashbackcijfer van de hele catalogus. De hele bescherming zou dan hangen
aan één Nederlandse formulering in één voorwaardentekst, in twee onafhankelijke
regexen tegelijk. Eén herformulering door een volgende lane en Obsidian staat
gratis bovenaan elke nettolijst. Bovendien is "wat kost deze kaart mét zijn 5%"
gewoon **onbekend** — de tierprijs heeft geen gedateerde bron — en onbekend is
nooit nul, ook niet als er een vangnet onder ligt.

De prijs die we ervoor betalen staat hier eerlijk: `holdingCostOfProduct` geeft nu
`no-source` in plaats van `needs-another-product`, dus het scherm zegt "wat dit
product kost, staat niet in onze bronnen" waar "de prijs die de bron noemt geldt
bovenop een ander product" preciezer was geweest. Nagemeten:
`crypto-com-prepaid-card-private-obsidian` → `unknown no-source`.

**Wat dit weer losmaakt:** zodra iemand een gedateerd stuk vindt met het
eurobedrag van Level Up Plus (EUR 3,99/maand) en Pro (EUR 24,99/maand), horen die
twee als eigen catalogusrijen erin, en dán kan de kaartrij eerlijk naar
`needs-another-product` met een verwijzing naar een bestaand product in plaats van
naar een regex. De sweep noteerde dat `crypto.com/document/<slug>` HTTP 403 geeft;
die 403 is niet omzeild en is de eerste plek om te kijken.

### 3. Zeven rekeningen en pakketten die nog geen rij hadden

Deze zeven kwamen op 21 augustus door de eis maar bleven liggen omdat de staging
ze aan een **betaalpasrij** van dezelfde bank hing. Dat is een verschuiving en
geen invulling: € 4,30 is de prijs van BasisPakket Betalen, niet van een pas, en
`accountCosts` koppelt een rij met `kind: "betaalpas"` nooit aan een rekening —
daar zou het bedrag dus onzichtbaar zijn geweest.

| nieuwe rij | soort | bedrag | eenheid | datum |
|---|---|---|---|---|
| `abn-basispakket-betalen` | betaalpakket | € 4,30 | per maand | 2026-01 |
| `rabo-standaard` | betaalpakket | € 3,45 | per maand | 2026-07-01 |
| `sns-compleet` | betaalpakket | € 5,35 | per maand | 2026-02-01 |
| `asn-bankrekening` | betaalrekening | € 4,00 | per maand | 2026-07-01 |
| `regiobank-plus-betalen` | betaalpakket | € 4,00 | per maand | 2026-02-01 |
| `knab-priverekening` | betaalrekening | € 6,00 | per maand | 2026-02-18 |
| `triodos-betaalrekening-18-22` | betaalrekening | € 0,00 | per maand | 2026-05 |

Ze staan in **beide** bestanden op dezelfde plek, direct vóór hun zusterproduct,
zodat `catalog.json` en `state.json` dezelfde lijst in dezelfde volgorde dragen —
`catalogArtifact.test.ts` toetst precies dat, en een rij die maar in één van de
twee staat gaat stuk bij de volgende sweep.

Twee ervan zijn geen duplicaat van iets dat er al stond, en dat is nagelopen:

- `abn-losse-betaalrekening` (ook € 4,30) zegt in zijn eigen voorwaarden dat zijn
  bedrag geldt "als je geen BasisPakket Betalen of Studenten Pakket hebt". Twee
  producten, hetzelfde bedrag.
- `asn-basisbankrekening` (ook € 4,00) is de wettelijke basisrekening: één
  rekeninghouder, geen roodstand. `asn-bankrekening` is de gewone.

`rabo-standaard` repareert bovendien een verwijzing die nergens heen wees: de
RaboCard draagt al de voorwaarde "bovenop de € 3,45 per maand van Rabo Standaard",
en dat product bestond niet in de catalogus.

#### Wat dat meteen rechtzet, en het is geen kleinigheid

Vóór deze samenvoeging kende de catalogus twee RegioBank-betaalrekeningen:
JongWijs en de Studentenrekening, allebei € 0,00. `resolveCost` vond daar een
`provider-consensus` in en meldde voor **elke** RegioBank-betaalrekening
"€ 0,00 per maand". Nagemeten, vóór en na:

```
vóór : RegioBank / Betaalrekening   known 0/maand via provider-consensus   (2 kandidaten)
na   : RegioBank / Betaalrekening   unknown product-unknown                (3 kandidaten)
```

Dat was een valse nul van precies de soort die regel 1 verbiedt: de gewone
RegioBank-rekening kost € 4,00 per maand, en de app zei dat hij gratis was omdat
alleen de twee jeugdvarianten in huis waren. Eén product erbij haalt hem weg. Een
rekening die zijn pakket wél bij naam noemt wordt nu gewoon herkend
(`RegioBank / Betaalrekening Plus Betalen` → `known 4/maand via product-name`).

### 4. Zes koersopslagen vastgepind

| rij | waarde | documentdatum | route |
|---|---|---|---|
| `revolut-premium-betaalpas` | 0% | 2026-07-09 | agent (r.jina.ai) |
| `revolut-metal-betaalpas` | 0% | 2026-07-09 | agent (r.jina.ai) |
| `american-express-corporate-card` | 2,5% | 2022-12-15 | agent (PDF via curl) |
| `gnosis-pay-card-direct-consumer` | 0% | 2026-08-12 | agent (Zendesk-API) |
| `paysafecard-prepaid-code-paysafewallet` | 3% | 2026-05-26 | provider-page |
| `tria-card` | 0% | 2026-06-14 | provider-page |

De twee Revolut-nullen dragen **de weekendclausule**, en niet als versiering: de
nul geldt op weekdagen, en in het weekend rekent Revolut 1%. Zonder die zin is de
0 niet waar. Elk plan heeft nu zijn eigen per-plan-URL in plaats van een gedeelde
`plus-fees`-pagina.

Bij `paysafecard` staat erbij dat 3% **niet het maximum** is: waar de euro
helemaal niet in de transactie voorkomt geldt 6,09%. En de tegenspraak over de
maandkosten (marketingpagina € 3, voorwaarden € 4) staat er ook in — dat is een
ander veld, maar wie dat veld ooit invult loopt er tegenaan.

`american-express-corporate-card` is de duidelijkste vondst van de spec: dezelfde
PDF stond al onder `american-express-corporate-gold-card`, terwijl de kop van dat
document "AMERICAN EXPRESS® CORPORATE CARD" luidt. Het juiste document stond in
huis, tegen het verkeerde product aan. Hetzelfde geldt voor Gnosis Pay: het
tarievenartikel zat aan `zeal-card-gnosis-pay-rails` (dat het document niet eens
noemt) en niet aan het product waar het over gaat.

### 5. Twee datums rechtgezet

| rij | veld | was | is | waarom |
|---|---|---|---|---|
| `american-express-corporate-gold-card` | `fxFeePct` | 2026-08-19 | 2022-12-15 | het URL-pad zegt `…/2022-12-15/…`, de PDF-CreationDate 7 december 2022 |
| `zeal-card-gnosis-pay-rails` | `fxFeePct` | 2026-04-27 | 2026-08-12 | `updated_at` uit de eigen Zendesk-API van het artikel |

De waarden zijn ongemoeid gelaten. Dit was geen overschrijving met iets zwakkers
maar het omgekeerde: de dag waarop wij keken is vervangen door de dag die het
document noemt. De spec noemt ze in §1b en zegt erbij dat ze in dezelfde pas
horen; de Corporate Gold zou anders 2026-08-19 dragen naast een Corporate Card
die 2022-12-15 draagt, uit hetzelfde bestand.

Dat de Corporate Gold nu vier jaar oud oogt, is geen verslechtering. Hij ís vier
jaar oud.

### 6. American Express Platinum: gedateerd binnengehaald, met een tegenspraak

De staging had € 75 per maand van de productpagina. Die pagina draagt geen datum
(alleen "Copyright © 2026") en strandde dus. De opdracht vraagt om te noemen welk
gedateerd document hem alsnog binnenhaalt — dat document is
`FEE_CONS_V7_240704_NL.pdf`, hetzelfde stuk dat de zeven andere
Amex-consumentenkaarten in deze catalogus dateert. Opgehaald (HTTP 200,
application/pdf, 60.311 bytes, 3 pagina's) en gelezen. De rij staat er letterlijk:

> The Platinum Card | € 65,00 per maand | Inclusief: 4 extra Green Cards
> | Extra Platinum kaart (maximum 1 kaart): € 10 per maand

**€ 65, niet € 75.** Het gedateerde stuk is aangehouden, want de eis schrijft de
documentdatum voor. Maar dat betekent dat dit cijfer vermoedelijk is achterhaald,
en dat staat met zoveel woorden in de voorwaarden van het veld: het is een
**ondergrens**, geen actuele prijs. De rest van beide bronnen spreekt elkaar niet
tegen (extra Platinum-kaart € 10 per maand, vier extra Green Cards inbegrepen op
allebei), dus het gaat om hetzelfde product met een verhoogde bijdrage.

Dit raakt zeven rijen tegelijk en hoort op de lijst van de eigenaar: **alle acht
Amex-consumentenkaarten in deze catalogus komen uit een PDF van juli 2024.** Als
de Platinum in twee jaar van 65 naar 75 is gegaan, is de kans groot dat Blue,
Green, Gold en de vier Flying Blue-kaarten ook zijn verschoven. Eén nieuwer
gedateerd `FEE_CONS`-versienummer vervangt ze alle acht in één keer.

De business-kaarten staan **niet** in dat document — het is de "American Express
Charge Card"-tabel voor consumenten. `american-express-business-green-card` blijft
daarom liggen.

---

## Wat er niet in ging, en wat eraan ontbrak

### Strandt op de datum (5 rijen)

| rij | bedrag | wat ontbreekt | welk gedateerd document hem alsnog binnenhaalt |
|---|---|---|---|
| `wise-rekening` | € 0,00/maand | `wise.com/nl/pricing` draagt geen datum | geen gevonden. Het wettelijk verplichte tarievendocument (`wise.com/pricing/fees-documents`) geeft **403** aan curl met browser-UA, en via r.jina.ai komt er een advertentiepixel van 160 bytes terug. Die 403 is genoteerd en niet omzeild. |
| `wise-betaalpas` | € 0,00/maand | idem | idem |
| `american-express-business-green-card` | € 85,00/jaar | productpagina zonder datum | **niet** `FEE_CONS_V7_240704_NL.pdf` — dat stuk dekt alleen de consumentenkaarten; gecontroleerd, alle drie de pagina's gelezen. Er is een gedateerd zakelijk tarievenblad nodig. |
| `asn-creditcard-student` | € 27,50/jaar | productpagina zonder datum; de ASN-tarievenwijzer noemt de studentenkaart niet | een tarievenwijzer die de studentenvariant wél noemt. Strandt bovendien op de rijtoewijzing, zie hieronder. |
| `crypto-com-level-up-plus` / `-pro` | € 3,99 resp. € 24,99/maand | `crypto.com/nl/cards` draagt geen enkele datum, geen `lastUpdated`, geen `__NEXT_DATA__` | de twee gedateerde Level Up-artikelen (12017604 van 18 juni 2026 en 12017612 van 14 juli 2026) beschrijven de plannen maar drukken het eurobedrag niet af. `crypto.com/document/<slug>` geeft 403 — dáár zou het staan. |

`paysafecard` (€ 3/maand activeringskosten) staat hier niet apart in de tabel maar
hoort er wel: de kostenpagina draagt geen datum, en de gedateerde algemene
voorwaarden noemen voor dezelfde post € 4 per maand na 30 dagen. Twee bedragen van
dezelfde uitgever. Dat is geen datumprobleem maar een tegenspraak, en die staat nu
in de voorwaarden van het `fxFeePct`-veld van dat product zodat de volgende hem
niet opnieuw hoeft te vinden.

### Strandt op de rijtoewijzing (4 rijen, alle vier mét een goede datum)

`abn-studenten-credit-card` (€ 1,31/maand, Januari 2026), `sns-creditcard-student`
(€ 27,50/jaar, 1 februari 2026), `regiobank-creditcard-student` (€ 27,50/jaar,
1 februari 2026) en `openbank-betaalpas-travelplus` (€ 4,99/maand, geldig vanaf
1 juli 2024).

Deze vier komen door de eis, en gaan er toch niet in. `catalogArtifact.test.ts`
bewaakt met zoveel woorden dat de catalogus per bank één betaalpas en één
creditcard kent:

```
for (const product of ["SNS creditcard", "RegioBank creditcard", "ASN creditcard", "Openbank betaalpas"])
  expect(catalogueCandidates(catalog.entries, product).map(...)).toHaveLength(1);
```

Een tweede creditcardrij bij dezelfde bank maakt `catalogueProductFor` ambigu, en
dan zegt Reizen "de catalogus kent meer dan één SNS creditcard en die rekenen niet
hetzelfde" — een bewering die geen van beide documenten doet. "SNS Creditcard bij
Studentenrekening" is dezelfde kaart onder een voorwaarde, geen tweede kaart.

De assertie noemt ABN niet — die bank heeft al twee creditcardrijen (gewoon en
Gold), en die worden op hun koersopslag uit elkaar gehouden. Het commentaar bóven
de assertie noemt "ABN AMRO Studenten Credit Card" wél met naam als een van de
vijf die om deze reden geen rij kregen, en die redenering geldt onverkort: een
derde ABN-creditcard zonder eigen koersopslag is niet meer te onderscheiden.

Dit is een beslissing voor de eigenaar en niet voor deze lane. Wil hij ze erin,
dan moet eerst `catalogueProductFor` een voorwaardelijke variant kunnen
onderscheiden van een apart product — en dan pas de rij.

Verder liggen nog vijf bedragen uit de kostensweep op ditzelfde punt vast:
`bunq-eerste-mastercard` (€ 0,00, "1 pas is gratis inbegrepen") en
`bunq-extra-mastercard` (€ 3,49/maand) tegen negen bunq-kaartrijen, en de drie
Plutus-abonnementen (€ 6,99 / € 9,99 / € 19,99) tegen één `plutus-card`. De
bunq-cijfers dragen een goede datum (03/08/2026); Plutus draagt er geen.

### Kaartkosten die geen kaartkosten zijn

De sweep leverde ook eenmalige en voorwaardelijke bedragen: bestelkosten van
Gnosis Pay (waarover twee gedateerde stukken van dezelfde uitgever elkaar
tegenspreken: 30,23 EURe versus FREE), vervangingskaarten, en de slaapkosten van
Crypto.com (EUR 5 per maand na twaalf maanden, EUR 6 per 1 oktober 2026). **Geen
daarvan is in `accountFee` beland.** Een voorwaardelijke maandpost is geen prijs;
ze staan in de voorwaardenteksten van de rijen waar ze bij horen.

### Drie schone negatieven uit de spec

`wise-betaalpas`, `wirex-card-wirex-one` en `bleap-card` hebben géén `fxFeePct`
gekregen, en dat is het antwoord en niet een gat:

- **Wise** heeft geen kaarttoeslag; wat het rekent is een conversievergoeding per
  corridor die nergens als één getal gepubliceerd staat ("From 0.2%"). Een 0 hier
  zou Wise elke reisrangschikking laten winnen tegen kaarten die eerlijk een
  percentage noemen, terwijl er een laag lager wél iets wordt gerekend.
- **Wirex** zegt in zijn eigen tarievendocument dat het cijfer in de app staat.
  Dat is een weigering op de bon.
- **Bleap** noemt in zijn gedateerde Fee Schedule kaartbesteding kosteloos, maar
  raakt valutaconversie nergens aan, en de kaart houdt alleen USD en EUR — een
  derde valuta wordt dus door Mastercard omgerekend tegen een onbenoemde opslag.

De spec stelt voor die drie een `lastReason` in `state.json` voor. **Niet gedaan**,
zie hieronder.

---

## Wat deze lane bewust niet heeft aangeraakt

`state.json` mocht alleen open voor het toevoegen van producten. De volgende
punten uit de spec liggen dus nog klaar en zijn niet gedaan:

1. **Drie `lastReason`-teksten** voor de schone negatieven (§3.1–3.3 van de spec).
2. **`readable` / `route` voor `revolut-premium-betaalpas` en
   `revolut-metal-betaalpas`**: die staan op `bot-blocked` met een gedeelde
   `plus-fees`-URL, terwijl beide via r.jina.ai volledig leesbaar zijn en nu elk
   hun eigen per-plan-URL hebben (die URL's staan wél in de nieuwe
   `fxFeePct`-velden).
3. **De `pointsReason`-herschrijving voor `ing-betaalpas`** plus de regel dat ING
   Punten geen eurowaardering mogen krijgen — ING zegt zelf "ING Punten hebben
   geen monetaire waarde en kunnen niet worden ingewisseld voor geld". Dat is een
   uitspraak van de uitgever onder de keuze die de Punten-tab al had gemaakt.
4. **Het `counts`-blok van `state.json`** staat nog op de ontdekkingssweep van 18
   augustus (`products: 124`), terwijl het bestand er nu 192 draagt. Het is
   expliciet een diff-anker en geen teller, dus het is met rust gelaten — maar wie
   het ooit als teller leest, leest iets verkeerds.

Verder onaangeroerd, met reden:

- **`american-express-blue-card`** staat in de catalogus op € 0,00 **per jaar**
  en in de staging op per maand. Bij een nul maakt het voor de som niets uit, het
  bestaande veld is gedekt, en een gedekt veld wint. Genoteerd, niet gewijzigd.
- **`american-express-corporate-gold-card.fxFeePct`** draagt `conditions: null`
  bij een opslag van 2,5% die in werkelijkheid een DCC-uitzondering en een aparte
  opnameprijs kent. Dat is een zwak maar gedekt veld; overschrijven viel buiten
  deze opdracht. De nieuwe Corporate Card-rij ernaast heeft die tekst wél, uit
  hetzelfde document, dus het verschil is nu zichtbaar.

---

## Stand van de catalogus

**192 producten** (was 185). Per veld:

| veld | vóór | na |
|---|---|---|
| `accountFee` | 89 | **102** |
| `fxFeePct` | 73 | **79** |
| `pointsPerEuro` | 58 | 58 |
| `interestPct` | 32 | 32 |
| `cashbackPct` | 8 | 8 |

Koersopslag over de kaartproducten (betaalpas, creditcard, prepaid, crypto):
**79 van de 86**. De spec rekende met 82; dat getal is inmiddels 86, want de ronde
van 21 augustus voegde drie ING-creditcards toe. Van de zeven open rijen zijn er
drie beantwoord (de schone negatieven), is er één geen kaart (`bunq-crypto` is een
handelsproduct) en zijn er drie de nieuwe ING-creditcards, die de spec nog niet
kende.

Rekeningkosten per soort: 33 betaalpakketten, 33 betaalrekeningen, 29 creditcards,
4 betaalpassen, 2 crypto-kaarten, 1 prepaid. De oudste is Wirex (2024-01-11), de
jongste bunq Pro Business (2026-08-03).

### Het getal waar deze lane om draaide

**Kaarten met ZOWEL een cashbackcijfer als een prijs: 2** (was 0).

- `bleap-card` — 1% cashback, € 0,00 per maand
- `wirex-card-wirex-one` — 0,5% Cryptoback, € 0,00 per maand

Van de zes andere cashbackrijen zijn er vier de Crypto.com-niveaus die hierboven
bewust geen prijs kregen, en twee de Gnosis Pay-kaarten, waarvoor geen enkel
gedateerd document een maand- of jaarprijs noemt (de sweep liep daarvoor 23
helpcentrum-artikelen af).

### En wat dat getal nog NIET doet — nagemeten, niet aangenomen

Twee kaarten met beide cijfers leveren **nul** nettovergelijkingen op in de
extensie, en de oorzaak zit in de code en niet in de gegevens. `rank.ts` regel 611:

```ts
const feeClaim = bepaalClaim(caveats, "kaartkosten");
if (feeClaim.soort !== "vast") return { ...gedeeld, basis: "bruto", ... };
```

`bepaalClaim` geeft alleen `"vast"` terug als er **geen enkele** caveat op dat
veld staat, en `leesVoorwaarden` eindigt met:

```ts
if (uit.length === 0) uit.push({ soort: "onbeoordeeld", veld });
```

Een niet-lege voorwaardentekst levert dus altijd minstens één caveat op, en dus
nooit `"vast"`. **Alle 102 `accountFee`-velden in de catalogus dragen een
voorwaardentekst** (geteld: nul zonder). Gemeten met de echte rijen, aankoop van
€ 100 in euro's:

```
echte voorwaarden          bleap-card             basis: bruto           netto: 100   charge: null
                           wirex-card-wirex-one   basis: voorwaardelijk  netto: null  charge: null
alle conditions op null    bleap-card             basis: netto           netto: 100   charge: 0
                           wirex-card-wirex-one   basis: netto           netto: 50    charge: 0
```

Elke test in `apps/extension/src` die de nettotak raakt gebruikt
`conditions: null`; op echte catalogusgegevens is `basis: "netto"` nog nooit
gehaald. Wirex komt bovendien niet eens tot de prijsvraag: zijn cashback heet
"Cryptoback", dus de opbrengst is niet in euro's en de rij eindigt op
`voorwaardelijk`.

De reparatie is geen datawerk. `options.ts` zegt het probleem zelf al hardop: "Bij
twintig van de zevenentwintig kaarten met een prijs is de voorwaardentekst een
herkomstnotitie … en geen beperking." `bepaalClaim` moet dat onderscheid kunnen
maken — een herkomstnotitie mag `"vast"` blijven, een beperking niet. Dat is een
beslissing voor de eigenaar en een andere lane; deze lane heeft de gegevenskant
afgemaakt en meet hier alleen wat er daarna nog tussen zit.

## Testen

`cd packages/core && npx vitest run`: **61 bestanden, 1021 tests, alles groen.**
Met dezelfde commit en de catalogus tijdelijk teruggezet: ook 61 / 1021 groen. De
samenvoeging voegt geen tests toe en breekt er geen.

`travel.test.ts` (80), `catalogRoutes.test.ts` (12), `fxRoutes.test.ts` (36),
`catalogArtifact.test.ts` (9), `accountCosts.test.ts` (48) en `netBenefit.test.ts`
(27) zijn alle zes groen, vóór en na.

Extra, buiten de opdracht maar het leest hetzelfde artefact:
`apps/server` → `src/catalogFile.test.ts`, 7 tests, groen.

Het aantal in `packages/core` liep tijdens deze sessie van 1015 naar 1021; dat komt
van commit `9fae0a3` van een andere lane (bankCsv en subscriptions), niet van deze
samenvoeging.

---
---

# Tweede ronde dezelfde dag: de kaarten van de acht grootbanken

Bron: `docs/catalog/staging-kosten-grootbanken.json` (24 augustus, 15:13) met het
verslag `docs/research/2026-08-24-kosten-grootbanken.md`. Doelbestand: alleen
`docs/catalog/catalog.json`. `state.json` is niet aangeraakt en hoefde niet — zie
onderaan.

Dit is een aparte samenvoeging dan de drie hierboven, op dezelfde dag. De ronde
van 14:44 gaf de acht grootbanken hun REKENINGEN en PAKKETTEN; wat toen leeg
bleef waren hun betaalpas- en creditcardrijen. Het werk hier was daarom
rijtoewijzing — hoort dit tarief bij de pas of bij het pakket — en niet zoeken.
Elke bank in deze groep publiceert een gedateerd, EU-verplicht Informatiedocument
of een gedateerde Tarievenwijzer, en daarom is de categorie "strandt op de datum"
deze keer leeg. Vijftien doorlopende bedragen gevonden, vijftien door de eis,
vijf erin.

De diff is **45 toegevoegde regels en 0 verwijderde**. Er is geen enkele waarde
overschreven en er is geen enkele regel vervangen; `git diff --stat` zegt
`45 +++++`.

## De toelatingseis

Dezelfde als hierboven, en opnieuw nagemeten in plaats van aangenomen: waarde +
bron + datum + voorwaarden, alle vier, en de datum is die van het **document**.
Alle vijf zijn door de echte `readAccountFee` gehaald, niet door een tabel op
papier:

```
abn-amro-betaalpas     1.5  / maand / 2026-01-01 / provider-pdf
ing-creditcard         2    / maand / 2026-01-01 / provider-pdf
ing-platinumcard       4.35 / maand / 2026-01-01 / provider-pdf
rabo-goldcard          2    / maand / 2026-07-01 / provider-pdf
knab-creditcard        28   / jaar  / 2025-11-01 / wayback
accountFees(): 107     geweigerd op de hele catalogus: []
```

**Er is nergens omgerekend.** Drie van de vijf documenten noemen zowel de maand-
als de jaarprijs — ABN "€ 1,50 per maand / € 18,00 per jaar", Rabobank "€ 2,00 per
maand / totale jaarlijkse vergoeding € 24,00" — en in alle drie is de eenheid van
de tariefregel aangehouden en staat het andere bedrag in de voorwaarden. Knab
staat op `jaar` omdat ICS zijn kaarten per jaar prijst. De staging schreef
`unit: "EUR per maand"`; dat is in de catalogus `period: "maand"` geworden, wat
hetzelfde is in ander vocabulaire en geen omrekening.

---

## Wat erin ging

### Vijf kaartprijzen, alle vijf bovenop iets anders

| rij | bedrag | eenheid | documentdatum | route | bron |
|---|---|---|---|---|---|
| `abn-amro-betaalpas` | € 1,50 | per maand | 2026-01-01 | `provider-pdf` | Informatiedocument BasisPakket Betalen |
| `ing-creditcard` | € 2,00 | per maand | 2026-01-01 | `provider-pdf` | Informatiedocument ING OranjePakket |
| `ing-platinumcard` | € 4,35 | per maand | 2026-01-01 | `provider-pdf` | Informatiedocument ING OranjePakket |
| `rabo-goldcard` | € 2,00 | per maand | 2026-07-01 | `provider-pdf` | Informatiedocument Rabo Standaard |
| `knab-creditcard` | € 28,00 | per **jaar** | 2025-11-01 | `wayback` | ICS-wijzigingspagina jaarbijdrage Knab |

`abn-amro-betaalpas` is de enige van de acht banken die zijn betaalpas apart
prijst, en dat is precies waarom hij erin kan: het bedrag geldt voor wie **geen**
BasisPakket Betalen en **geen** Studenten Pakket heeft. Binnen die twee pakketten
zit de pas in de pakketprijs, en dan is er geen prijs voor de pas.

`knab-creditcard` spiegelt `asn-creditcard` regel voor regel: dezelfde
documentfamilie (een ICS-wijzigingspagina die ICS weghaalt zodra de volgende
verhoging is aangekondigd), dezelfde Wayback-`id_`-vorm, dezelfde route
`wayback`, en de datum uit de zin zelf ("vanaf 1 november 2025") en niet de
Wayback-stempel van 15 april 2026.

**Alle vijf komen uit op `needs-another-product`, en dat is de bedoeling.**
Gemeten met de echte `holdingCostOfProduct`:

```
abn-amro-betaalpas   pricedOnItsOwn=false  openToNew=true  {"kind":"unknown","reason":"needs-another-product"}
ing-creditcard       pricedOnItsOwn=false  openToNew=true  {"kind":"unknown","reason":"needs-another-product"}
ing-platinumcard     pricedOnItsOwn=false  openToNew=true  {"kind":"unknown","reason":"needs-another-product"}
rabo-goldcard        pricedOnItsOwn=false  openToNew=true  {"kind":"unknown","reason":"needs-another-product"}
knab-creditcard      pricedOnItsOwn=false  openToNew=true  {"kind":"unknown","reason":"needs-another-product"}
```

### En hier is de prijs van die vijf keer `false`, eerlijk opgeschreven

**De hele bescherming hangt aan twee woorden.** `PRICE_NEEDS_ANOTHER_PRODUCT`
kent drie alternatieven; nagemeten slaat bij alle vijf alléén het laatste aan, en
alle vijf op dezelfde twee woorden:

```
abn-amro-betaalpas   match: true  letterlijk: "Bovenop de"  alternatieven: ["bovenop de"]
ing-creditcard       match: true  letterlijk: "Bovenop de"  alternatieven: ["bovenop de"]
ing-platinumcard     match: true  letterlijk: "Bovenop de"  alternatieven: ["bovenop de"]
rabo-goldcard        match: true  letterlijk: "Bovenop de"  alternatieven: ["bovenop de"]
knab-creditcard      match: true  letterlijk: "bovenop de"  alternatieven: ["bovenop de"]
```

Geen van de vijf productnamen bevat "bij", dus de korte sluiting van
`isPricedOnItsOwn` op de PRODUCTNAAM doet hier niets — het is de voorwaardentekst
en niets anders.

Dat is hetzelfde mechanisme dat §2 hierboven bij Crypto.com **niet** wilde
gebruiken, en het verschil moet erbij staan. Bij Crypto.com zou de regex een
**nul** moeten beschermen die als "gratis en 5% cashback" bovenaan elke nettolijst
kan komen. Hier beschermt hij vijf **echte, positieve** bedragen tegen een te
lage lezing: valt de regex weg, dan staat er € 1,50 / € 2,00 / € 4,35 / € 28,00
in plaats van "onbekend", en dat is te laag maar niet gratis. De faalrichting is
dus minder erg — maar hij is er wel, en herformuleren van "Bovenop de" in een van
deze vijf teksten laat Optimalisatie een kaart aanraden waarvan de prijs alleen
bestaat bovenop een pakket van € 3,45 tot € 6,00. **Wie een van deze vijf
voorwaardenteksten herschrijft, moet "bovenop de" laten staan.**

### Wat dit op het scherm verandert, nagemeten

`accountCosts` over twaalf synthetische rekeningen (betaalrekening + creditcard
bij elk van de acht banken), vóór tegen na:

```
gewijzigd:
ING/Creditcard    unknown product-unknown [kandidaten 3]  =>  known 2/maand  via product-name (ing-creditcard)
Knab/Creditcard   unknown product-unknown [kandidaten 0]  =>  known 28/jaar  via product-name (knab-creditcard)

ongewijzigd (o.a.):
Rabobank/Creditcard   known 2/maand via product-name (rabobank-creditcard)
ABN AMRO/Creditcard   known 2.55/maand via product-name (abn-amro-creditcard)
alle acht betaalrekeningrijen: unknown product-unknown
```

Twee rijen gaan van "onbekend" naar een bedrag met een bron. Er is **geen enkele
valse nul** bijgekomen en **geen enkele nieuwe `provider-consensus`**: Rabobank
heeft nu twee creditcardrijen op precies € 2,00 per maand, maar `Rabobank/Creditcard`
blijft via `product-name` op `rabobank-creditcard` uitkomen, dus de consensustak
wordt niet geraakt. `productFeesById` gaat van 118 naar 123, en die vijf zijn
exact de vijf rijen zelf.

**Reizen verschuift geen enkele bucket.** `netBenefit` geeft bij
`no-source` en bij `needs-another-product` dezelfde `gross-cost-unknown` met
hetzelfde brutobedrag; alleen de zin verandert, van "wat dit product kost, staat
niet in onze bronnen" naar "de prijs die de bron noemt geldt bovenop een ander
product". Preciezer, niet anders gerangschikt.

---

## De zeven betaalpassen die GEEN bedrag krijgen — en waar hun reden nu staat

Zeven van de acht banken zeggen in hun eigen gedateerde document dat de eerste
betaalpas in de pakketprijs zit. Dat is **geen nul**. Een nul daar zegt "een
ING-betaalpas is gratis", terwijl de enige manier om er een te hebben een pakket
van € 4,00 per maand is.

**De opdracht was deze zeven een reden te geven zonder bedrag, in de vorm die
eerdere rondes gebruikten. Die vorm is deze doc — er is geen veld voor.** Dat is
nagemeten en niet aangenomen:

- `catalog.ts` zet `CatalogValue.value: number` (geen `number | string`) en
  `isCovered` weigert op `!Number.isFinite(v.value)`. Er is in
  `fields.accountFee` geen plek voor een reden in plaats van een bedrag.
- `state.json` draagt over 192 producten 26 veldnamen en géén `feeReason` of
  `accountFeeReason`. `pointsReason` bestaat (9 producten) en is het enige
  veldspecifieke-reden-precedent, maar een `feeReason` bijverzinnen is een nieuw
  veld, en dat mocht niet.
- Het precedent staat hierboven in §2: de vier Crypto.com-niveaus kregen
  precies om deze reden geen `accountFee`, en hun reden staat in dit document en
  nergens anders.

**Eén ding erbij, en het corrigeert de staging.** De staging schrijft zeven keer
"de honest landing is `{ kind: 'unknown', reason: 'needs-another-product' }`".
Dat is niet wat een leeg veld doet: `netBenefit.ts` geeft `if (!fee) return
{ kind: "unknown", reason: "no-source" }`. Leeg laten geeft **`no-source`**, dus
het scherm zegt "staat niet in onze bronnen" en niet "geldt bovenop een ander
product". Leeg laten is nog steeds juist — `needs-another-product` vraagt een
getal plus een regexmatch, en dat is exact de route die §2 heeft afgewezen — maar
de zin die de lezer krijgt is de andere.

| rij | wat het document zegt | citaat |
|---|---|---|
| `ing-betaalpas` | OranjePakket € 4,00/maand "met inbegrip van een dienstenpakket" | "OranjePakket \| Per maand € 4,00 \| Met inbegrip van een dienstenpakket dat bestaat uit: … • betaalpas" (1 januari 2026) |
| `rabobank-betaalpas` | Rabo Standaard € 3,45/maand inclusief 1 betaalpas | "Aanhouden van de betaalrekening [Rabo Standaard] \| Per maand € 3,45 … • 1 Betaalpas [Rabo WereldPas]" (1 juli 2026) |
| `sns-betaalpas` | SNS Basis € 4,00 inclusief 1 betaalpas | "SNS Basis € 4,00 \| z 1 betaalpas"; bij SNS Compleet € 5,35 "Inclusief passen voor alle rekeninghouders en gemachtigden" (1 februari 2026) |
| `asn-betaalpas` | € 4,00 per kalendermaand, "Dit is inclusief één betaalpas" | "Aanhouden van de betaalrekening \| € 4,00 per rekening per kalendermaand (€ 48,00 per jaar). Dit is inclusief één betaalpas." (1 juli 2026) |
| `regiobank-betaalpas` | Plus Betalen € 4,00/maand "inclusief … een betaalpas" | "Betaalrekening Plus Betalen \| • Aanhouden van de betaalrekening, inclusief … een betaalpas … \| € 4,00 per maand" (1 februari 2026) |
| `knab-betaalpas` | pas, vervangende pas én extra pas zitten alle drie in het pakket | "Aanhouden van de betaalrekening \| Met inbegrip van een dienstenpakket … Aanbieden van een betaalpas \| Vervangende betaalpas \| Extra betaalpas — Privérekening: € 6 \| Gezamenlijke rekening: € 7" (01-10-2025) |
| `triodos-betaalpas` | rekening met "een dienstenpakket dat bestaat uit: - Een betaalpas" | "Rekening voor één persoon van 18 tot en met 22 jaar: € 0,00 per maand \| 23 t/m 25 jaar: € 3,50 \| vanaf 26 jaar: € 5,00" (mei 2026) |

Knab is het enige echte antwoord in plaats van een gat: daar zit ook de
**vervangende** en de **extra** pas in het pakket, dus er is geen losse pasprijs
te vinden — niet omdat er niet is gekeken.

### Drie valstrikken die hier NIET zijn ingevuld, met naam en toenaam

Deze horen woordelijk in dit document, zodat de volgende lane ze niet opnieuw
"repareert":

1. **Rabobank, de digitale pas.** "Tarieven en limieten" (december 2025) zet in
   de kolom "Tarief per maand" de regel **"Digitale betaalpas € 0,00"**. Dat is
   een uitgesproken nul, maar hij gaat over de DIGITALE pas (Apple Pay, Garmin
   Pay, Fitbit Pay, Google Pay — voetnoot 1) en niet over de fysieke Rabo
   WereldPas waar `rabobank-betaalpas` over gaat. Diezelfde tabel prijst wél een
   "Betaalpas bij KeuzePlus Hypotheek" op € 1,40 per maand; die hangt aan een
   hypotheek en is deze rij dus ook niet.
2. **ASN, "Eerste verstrekking betaalpas: € 0".** Gedateerd en uitgesproken, en
   toch niet gebruikt: het prijst de VERSTREKKING (een eenmalige handeling) en
   zwijgt over het AANHOUDEN. Dat is letterlijk de NIBC/Ayvens/DHB-valstrik.
3. **RegioBank en Triodos, de rekeningnullen.** RegioBank JongWijs € 0,00 en de
   Studentenrekening € 0,00, beide met de formulering "inclusief … een
   betaalpas"; Triodos € 0,00 per maand voor 18 t/m 22 jaar. Alle drie zijn de
   prijs van de REKENING en niet van de pas — dezelfde Triodos-pas kost bij een
   26-jarige € 5,00 per maand aan rekening. Dit is exact de valse nul die de
   ronde van 14:44 bij RegioBank heeft weggehaald door de gewone rekening als
   eigen rij toe te voegen.

---

## Wat er verder niet in ging, en waarom

### Zes echte extra-pasprijzen zonder catalogusrij

Alle zes gedateerd, alle zes doorlopend, alle zes zonder rij om in te landen: de
catalogus kent per bank één betaalpasrij en die gaat over de pas van de
rekeninghouder zelf.

| product | bedrag | documentdatum | waarom eruit |
|---|---|---|---|
| ING extra betaalpas voor gemachtigde | € 1,20/maand | 1 jan 2026 | geen rij; het Kostenoverzicht (15 juni 2026) noemt hetzelfde bedrag voor de tweede rekeninghouder bij een en/of-rekening |
| Rabobank extra betaalpas [Rabo WereldPas] | € 1,20/maand | 1 jul 2026 | geen rij, én het bedrag hangt aan het pakket: in Rabo Comfort is dezelfde pas € 0,00 |
| SNS extra betaalpas | € 1,35/maand | 1 feb 2026 | geen rij, en drie bedragen voor dezelfde handeling (Basis € 1,35, Compleet € 0,00, overige € 1,35) |
| ASN extra betaalpas | € 1,35/maand | 1 jul 2026 | geen rij; het document zegt "per kalendermaand", en dat is de bewoording van de bron |
| RegioBank extra betaalpas | € 1,35/maand | 1 feb 2026 | geen rij |
| Triodos extra betaalpas voor gemachtigde | € 4,00/maand | mei 2026 | geen rij; het duurste extra-pastarief van de acht, even duur als de hele rekening van een 26-plusser |

Vier van de zes staan al ergens, nagemeten in de bestaande voorwaardenteksten:
`sns-basis` ("Extra betaalpas € 1,35 per maand (bij SNS Compleet € 0,00)"),
`sns-compleet`, `asn-bankrekening` ("€ 1,35 per kalendermaand") en
`regiobank-plus-betalen`.

Bij de laatste twee is het preciezer dan "staat nergens", en dat hoort er zo bij
te staan:

- **ING € 1,20 staat er wél, maar voor een andere handeling.** `ing-go`,
  `ing-more`, `ing-extra` en `ing-max` dragen alle vier "Tweede rekeninghouder
  bij een en/of-rekening kost € 1,20 per maand extra (inclusief betaalpas)". Het
  bedrag uit de staging is de "extra betaalpas voor **gemachtigde**" — hetzelfde
  cijfer uit hetzelfde document, maar een andere regel. ING zet beide op € 1,20.
- **Rabobank € 1,20 en Triodos € 4,00 staan werkelijk nergens.**
  `rabo-standaard` noemt zijn extra WereldPas niet (alleen de
  buitenlandtoeslag), en geen van de vier Triodos-rekeningrijen noemt de extra
  pas voor een gemachtigde. `rabo-basispakket` noemt "extra rekeninghouder
  € 1,20", maar dat is een ouder pakket uit december 2025 en een andere post.

Deze bedragen aan de voorwaarden van een buurrij toevoegen zou een bestaande
waarde wijzigen — en het is tekst die `openToNewCustomers` en `isPricedOnItsOwn`
lezen — dus dat is hier niet gedaan. Voor die twee is dit document de enige plek
waar ze bestaan.

### Vier keer apart-product-of-variant, en alle vier al afgedekt

| voorstel | bedrag | het antwoord van de bank zelf |
|---|---|---|
| `abn-studenten-credit-card` | € 1,31/maand | de bank doet het ALLEBEI en spreekt zichzelf tegen: het tariefdocument zegt variant (regel heet "ABN AMRO Credit Card", binnen het blok Studenten Pakket), de website zegt apart product (eigen naam, eigen pagina, eigen inkomenseis € 500 i.p.v. € 1.500, lager limiet). Enige van de vier waar de aparte-productlezing verdedigbaar is — en toch geen rij: een derde ABN-creditcard draagt dezelfde 2% koersopslag als `abn-amro-creditcard`, en dat is het onderscheid waarop de bestaande twee uit elkaar worden gehouden. |
| `sns-creditcard-student` | € 27,50/jaar | variant, en de bank zegt het in het woordje "bij". Eén kop, één kaartnaam, twee prijzen, en het verschil is de REKENING. `isPricedOnItsOwn` zou de naam "SNS Creditcard **bij** Studentenrekening" trouwens meteen als "hangt aan een ander product" markeren. |
| `regiobank-creditcard-student` | € 27,50/jaar | variant, de duidelijkste: één kop "Aanbieden van DE RegioBank Creditcard door ICS" en daaronder twee bullets die geen kaarten zijn maar REKENINGEN. |
| `openbank-betaalpas-travelplus` | € 4,99/maand | variant, en Openbank sluit het zelf: "Het pasnummer, de geldigheidsdatum en de CVC zijn in beide gevallen dezelfde." Zelfde plastic met een schakelaar. |

Alle vier staan hun bedrag al in de `conditions` van de bestaande rij
(`abn-amro-creditcard`, `sns-creditcard`, `regiobank-creditcard`,
`openbank-betaalpas-r42-betaalpas`). Dit is dezelfde beslissing als hierboven
onder "Strandt op de rijtoewijzing", en ze blijft bij de eigenaar: eerst moet
`catalogueProductFor` een voorwaardelijke variant kunnen onderscheiden van een
apart product, dan pas de rij. `openbank-betaalpas-travelplus` mag helemaal van
de lijst af — die is volledig afgedekt.

### Vier schone negatieven

- **Triodos heeft geen creditcard.** Vier regels in het EU-verplichte document
  zeggen het los van elkaar: "Aanbieden van een creditcard \| Dienst niet
  beschikbaar", idem voor opnemen en voor betalen in vreemde valuta. Dat de
  catalogus geen `triodos-creditcard` kent is dus juist, en de eis "per bank één
  betaalpas en één creditcard" kan hier per definitie niet worden gehaald.
- **Knab noemt de jaarbijdrage niet in zijn eigen documenten**, en zegt zelf naar
  wie je moet: "De Knab Creditcard is een product van International Card Services
  (ICS). … alle kosten met betrekking tot je creditcard betaal je aan hen. Daarom
  staan die kosten niet in dit overzicht." Een weigering op de bon én een
  routewijzer — en die route heeft de € 28,00 hierboven opgeleverd.
- **ING Studenten Creditcard More heeft in geen enkel ING-document een
  cardfee.** Ongewijzigd sinds 21 augustus. Onbekend, niet nul.
- **`rabobank.nl` geeft HTTP 403** op een gewone curl met browser-UA (18.728
  bytes foutpagina). Genoteerd, niet omzeild, en niet nodig:
  `media.rabobank.com` geeft alle Rabobank-documenten wel met HTTP 200.

---

## Twee bevindingen die groter zijn dan deze samenvoeging

### De catalogus beweert nu meer ING-creditcards dan ING verkoopt

Na deze ronde staat `ing-creditcard` op € 2,00/maand naast
`ing-creditcard-more` op € 2,00/maand, en `ing-platinumcard` op € 4,35/maand
naast `ing-creditcard-extra` op € 4,35/maand. Vier bedragen, paarsgewijs
identiek, uit twee ING-documenten die elkaars kaartnamen niet kennen: het
Informatiedocument van 1 januari 2026 kent alleen "Creditcard" en "Platinumcard",
het Kostenoverzicht van 15 juni 2026 kent alleen "ING Creditcard More/Extra/Max".
Het verslag leest dat als drie kaarten, niet vijf.

Vandaag is dat nagemeten onschadelijk — `ING/Creditcard` komt via `product-name`
op `ing-creditcard` uit, dus het eurobedrag is goed, en vijf onderling oneens
zijnde kandidaten houden de betaalrekeningrijen op `product-unknown`. Maar de
verdubbeling is nu zichtbaar op een PRIJSscherm in plaats van alleen op een
koersopslagscherm. Samenvoegen van die rijen is een eigenaarsbesluit en geen
meting.

### Eén document, twee precisies in één artefact

`abn-basispakket-betalen` draagt `checkedAt: "2026-01"` voor
`informatiedocument-vergoedingen-basispakket-betalen.pdf`, met in zijn
voorwaarden de bewering dat dat document "alleen de maand" noemt.
`abn-amro-betaalpas` draagt vanaf nu `"2026-01-01"` voor **dezelfde URL**, met het
citaat "Datum: 1 januari 2026". Beide komen door `catalogArtifact.test.ts` heen.
Welke van de twee juist is, is niet uitgezocht — de bestaande waarde wijzigen mocht
niet — maar het artefact draagt nu twee precisies voor één PDF, en dat hoort
opgelost te worden door wie het document opnieuw openslaat.

---

## Twee dingen die niet in dit bestand konden, en dus hier staan

1. **De € 28,00 van Knab heeft een bekende einddatum.** Dezelfde ICS-pagina
   kondigt in zijn huidige versie al € 31,00 per jaar aan vanaf **1 november
   2026**, ook voor de Extra Card. Dat staat nu in de `conditions` van de rij,
   maar het hoort met een oktoberdatum in `docs/catalog/watchlist.md`, en dat
   bestand lag buiten deze lane.
2. **De voorwaardentekst van `abn-amro-betaalpas` noemt een codenaam.** De
   staging schreef "… en daarom is pricedOnItsOwn false", en die tekst is
   verbatim overgenomen omdat inkorten van een voorwaardentekst hier de prijs
   verandert. Het is wel de eerste keer dat een `conditions` in dit artefact een
   identifier uit `accountCosts.ts` bevat (geteld: nul andere). Herschrijven mag —
   **maar "Bovenop de" moet blijven staan.**

---

## Stand van de catalogus

**192 producten** (ongewijzigd; deze ronde voegt geen product toe). Per veld,
gemeten op `git show HEAD:docs/catalog/catalog.json` tegen het bestand:

| veld | vóór (na 14:44) | na |
|---|---|---|
| `accountFee` | 102 | **107** |
| `fxFeePct` | 79 | 79 |
| `pointsPerEuro` | 58 | 58 |
| `interestPct` | 32 | 32 |
| `cashbackPct` | 8 | 8 |

Per soort: `creditcard` 29 → **33**, `betaalpas` 4 → **5**, en
`betaalpakket` 33, `betaalrekening` 33, `prepaid` 1, `crypto` 2 ongewijzigd. Van
de 107 `accountFee`-velden draagt er nul geen voorwaardentekst.

`state.json` is **niet** aangeraakt en hoefde niet: er komt geen product bij, de
192 ids staan in dezelfde volgorde in beide bestanden (nagemeten: `JSON.stringify`
van de idlijsten is identiek), en `product`, `issuer` en `kind` van de vijf rijen
waren al gelijk in beide bestanden. Dat volgt het precedent van 14:44 exact: die
ronde raakte `state.json` alleen aan voor NIEUWE producten (140 toevoegingen, 0
verwijderingen), en bestaande rijen die die dag alleen een `accountFee` erbij
kregen zijn daar niet aangeraakt. `lastChecked` is met rust gelaten — dat veld is
van de weekelijkse sweep en staat op alle 192 producten op 2026-08-24.

## Testen

Deze sectie is na de samenvoeging herschreven. De eerste versie beschreef een rode
boom die niet meer bestaat en sloot af met een `git checkout` die de boom van groen
naar rood zou zetten. Wat er nu staat is de eindmeting, met turbo's cache
geforceerd omzeild zodat er niets op vertrouwen staat.

- `npx turbo run test --force` (root) -> **8 van 8 taken groen, 0 gecached:
  216 bestanden, 2743 tests, nul rood.** Per pakket: core 1021, web 932,
  extension 327, server 160, adapters 154, email-worker 56, investing-server 50,
  investing-web 43.
- `npx turbo run typecheck --force` (root) -> **8 van 8 groen, 0 gecached.**
- `cd apps/extension && npm run build` -> schoon, inclusief de eigen poorten:
  hostrechten gelijk aan het manifest, en geen netwerkverkeer in de bundel.

Drie beweringen uit de eerste versie van deze sectie zijn daarmee weerlegd.

- **`@lavega/extension` is niet rood.** De vier assertieregels in
  `src/rank.test.ts` zijn wel bijgewerkt, in de lane die de netto-tak repareerde:
  `CHECKOUT_CARDS` 77 -> 81, `metPrijs` 27 -> 38, `metAllebei` van `[]` naar
  `["bleap-card","wirex-card-wirex-one"]` met een herschreven kop, en
  `"onbeoordeeld"` toegevoegd aan de soortenlijst.
- **`@lavega/web` valt niet om onder acht parallelle vitest-processen.** Zonder
  `--concurrency=1`: 932 van 932 groen. Dat gedrag reproduceerde niet.
- **Het `git checkout`-advies is weg.** Het zou de bundel terugzetten op de
  catalogus van 21 augustus terwijl `rank.test.ts` nu 81 en 38 verwacht. Dat maakt
  de boom rood, en het is precies de tegenspraak die deze sectie zelf als faalvorm
  beschreef.

### De extensiebundel

`cd apps/extension && npm run bundle:catalog` gedaan. `CATALOG_GENERATED_AT` van
**"2026-08-21"** naar **"2026-08-24"**, en de kop van `77/73fx/8cb/51pt/27fee`
naar `81/79fx/8cb/58pt/38fee/2 met allebei`. Alle vijf nieuwe rijen komen met
waarde, periode, bron, datum en voorwaarden mee, en `leesVoorwaarden` geeft op geen
van de vijf teksten `"onbeoordeeld"`.

### Wat hier NIET gemeten is

De vijf bedragen zelf. Geen enkele lane heeft in deze ronde een bronstuk opnieuw
geopend: waarde, eenheid, documentdatum en citaat rusten volledig op
`staging-kosten-grootbanken.json` en `docs/research/2026-08-24-kosten-grootbanken.md`.
Dat is het grootste ongeverifieerde oppervlak van deze samenvoeging — vijf prijzen
die vanaf nu op een kostenscherm staan, uit documenten die in deze ronde niemand
heeft opgeslagen.

---

# Tweede ronde van 24 augustus: de FX-gaten van zeven kaarten

Aanleiding was geen sweep maar een REGEL UIT PRODUCTIE. In het opstartlog van
`@lavega/web` op Railway staat:

```
catalogue: 79 accepted, 113 refused of 192 for NL|USD
```

Dat leest alarmerend en was het niet. `ingestCatalogue`
(`apps/server/src/cardTerms.ts:335`) leest ÉÉN veld: `fxFeePct`. Gemeten met de
echte loader over de echte catalogus:

| | |
|---|---|
| fxFeePct GEDEKT (`isCovered`) | 79 |
| fxFeePct-veld ONTBREEKT | 113 |
| fxFeePct aanwezig maar door de poort afgewezen | **0** |

Die nul is het antwoord: de poort weigerde niets dat er wél was. En 106 van de 113
kunnen per definitie geen koersopslag hebben — 34 spaarrekeningen, 34
betaalrekeningen, 33 betaalpakketten en 5 beleggingsrekeningen. Een spaarrekening
wisselt geen valuta; de pas eraan wel.

Bleven over: **zeven producten die een kaart zijn die reist en geen cijfer
droegen.** Vier konden erin, drie hoorden leeg te blijven.

## De vier die erin gingen

| rij | waarde | documentdatum | route | bron |
|---|---|---|---|---|
| `ing-creditcard-more` | 2,00% | 15 juni 2026 | provider-pdf | ING Kostenoverzicht betaalproducten particulieren |
| `ing-creditcard-extra` | 0,00% **tot € 1.000 p/m, daarboven 2,00%** | 15 juni 2026 | provider-pdf | idem |
| `ing-creditcard-max` | 0,00% | 15 juni 2026 | provider-pdf | idem |
| `bleap-card` | 0,00% eigen opslag | 28 juni 2026 | provider-page | Bleap Cardholder Terms EEA, artikel 6 |

Vier dingen zijn hier het vermelden waard.

**HET DOCUMENT LAG ER AL.** De drie ING-varianten citeerden dat kostenoverzicht al
voor hun `accountFee`, met dezelfde datum. Er is dus geen nieuwe bron gevonden maar
een bestaande bron verder uitgelezen. Bij Bleap gold hetzelfde: de `accountFee`
komt uit artikel 6.1 van dezelfde voorwaarden, en de koersopslag staat in 6 en 6.3.

**DE VOORWAARDELIJKE NUL VAN DE EXTRA IS GEEN NUL.** De tabel zet hem op
"tot € 1000\*\*\* per maand 0,00%", en noot \*\*\* zegt: "Tot EUR 1000 geen
koersopslag per maandcyclus per creditcard contract, daarboven 2,00%". Het plafond
reist mee in `conditions`, precies zoals bij `ing-platinumcard` — en om precies de
reden die in `cardTerms.ts` staat opgeschreven: Revolut's 0% was alleen waar binnen
een plafond van € 1.000, werd onvoorwaardelijk getoond, stond bovenaan, en vertelde
hem dat de reis gratis was.

**DE NUL VAN DE MAX IS WÉL ONVOORWAARDELIJK, en dat is gemeten in plaats van
aangenomen.** Zijn cel draagt het plafond niet en de noot niet, en de opnamesectie
zegt onafhankelijk daarvan "Vreemde valuta opnemen: 4,00% + 0,00% koersopslag",
ook zonder plafond. Twee plekken, beide zonder grens. Wat er wel bij hoort staat in
de voorwaarden: de kaart bestaat alleen binnen het ING Max-pakket van € 44,99 per
maand.

**EEN NAAMVAL DIE BIJNA EEN VERKEERD CIJFER OPLEVERDE.** Hetzelfde document zet de
ING **Studenten** Creditcard More op "in vreemde valuta tot € 500 per
creditcardperiode 0,00%" en daarboven 2,00%. Dat is een ANDER product dan de ING
Creditcard More, die vlak op 2,00% staat. Wie op de woorden "Creditcard More"
zoekt, vindt eerst de verkeerde. Dat staat nu in de voorwaarden van de rij zelf.

Deze vier zijn de **eerste `fxFeePct`-rijen met route `provider-pdf`** in de hele
catalogus. Alle 79 die er al stonden zijn `agent` (71), `wayback` (6) of
`provider-page` (2). Ze claimen daarmee de hoogste precisietrap, en dat is juist:
het is ING's eigen gedateerde document.

## De drie die leeg blijven, elk met een gemeten reden

**`wise-betaalpas` — Wise publiceert geen kaart-FX-percentage.** Al vastgesteld in
`docs/BACKLOG.md` §5.3 en niet opnieuw geprobeerd: `wise.com/nl/pricing` geeft 200
maar noemt geen percentage per corridor, `card-fees` rekent live, en het wettelijk
gestandaardiseerde tarievendocument geeft 403. Een nul zou hier de gevaarlijkste
fout van de hele catalogus zijn: Wise zou elke reisranglijst winnen van kaarten die
hun percentage eerlijk opschrijven, terwijl er een laag lager wél wordt gerekend.
De eerlijke ingang is `api.wise.com/v4/comparisons` als LIVE bron, zoals de
Valuta-tab de ECB al gebruikt — niet een cijfer in dit bestand.

**`wirex-card-wirex-one` — geen bereikbare bron.** Het artikel dat de catalogus
voor deze rij al citeert (`help.wirexapp.com/article/x-tras-pricing-plans-and-tiers-1337`,
HTTP 200, 610 kB) bevat **nul** treffers op FX, foreign exchange, exchange fee,
conversion of OTC; het gaat alleen over abonnementsprijzen en Cryptoback. Drie
andere paden op 24 augustus 2026 gemeten: `wirexapp.com/en/fees`,
`wirexapp.com/fees` en `help.wirexapp.com/article/wirex-fees-1263` geven alle drie
**HTTP 404**.

**`bunq-crypto` — verkeerd veld, geen gat.** Dat is handelen in crypto via Payward
Europe Solutions (Kraken), geen betaalkaart. Er is geen kaarttransactie waarop een
koersopslag van toepassing is. Een cijfer hier zou de travel-block een
handelsproduct als reiskaart laten rangschikken.

## Wat het scherm ervan merkt

De echte loader, gedraaid zoals productie hem draait:

```
catalogue: 83 accepted, 109 refused of 192 for NL|USD
```

Na de deploy staat dat getal dus in zijn eigen opstartlog. En de extensiebundel
groeide van 81 naar 84 kaarten waarmee je kunt afrekenen, met kaartkosten van 38
naar 41 — de drie ING-varianten zijn nu afrekenbare kaarten. De tripwire in
`apps/extension/src/rank.test.ts` viel daarop om, zoals bedoeld, en is bijgewerkt
met de reden erbij.

`docs/catalog/catalog.json`: **77 toegevoegde regels, nul verwijderde.** Geen
bestaande waarde aangeraakt. `state.json` niet aangeraakt: er kwamen geen producten
bij.

## Wat deze ronde NIET heeft gedaan

**De Amex-familie is niet aangeraakt en is nu het oudste cluster in de catalogus.**
Elf FX-cijfers staan op `2022-03-01`: Blue, Green, Gold, Platinum en de vier Flying
Blue-kaarten, plus Business en Corporate op 2022-12 en 2023-03. Vier en een half
jaar oud, allemaal route `agent`. En dat is uitgerekend de uitgever waar álle
veertien puntencijfers groter dan nul van komen. Dat is één sweep-lane waard, en
het is niet deze.

`ing-creditcard` (de ICS-kaart, niet de drie ING Bank-varianten) staat op
`2022-10-01`, ook `agent`. Het kostenoverzicht van 15 juni 2026 dekt hem NIET — die
tabel gaat over More, Extra en Max, en `ing-creditcard` heeft ICS als uitgever. Er
is dus niets aan te upgraden zonder een eigen ICS-document.

En één inconsistentie die is blijven staan omdat huisregel 4 hem beschermt:
`ing-betaalpas` draagt `route: "agent"` voor een cijfer dat uit exact hetzelfde
gedateerde ING-providerdocument komt als de vier hierboven. Dat is een
onderwaardering, geen fout in de waarde.
