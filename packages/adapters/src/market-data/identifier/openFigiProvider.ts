import type { Provider } from "../providerRouter.js";
import type { IdentifierProviderResult, IdentifierRequest } from "../lanes.js";
type OpenFigiClient = { postJson(url: string, body: unknown): Promise<unknown> };
export function createOpenFigiIdentifierProvider(input: { client?: OpenFigiClient } = {}): Provider<IdentifierRequest, IdentifierProviderResult> {
  const cache = new Map<string, IdentifierProviderResult>(); const postJson = input.client?.postJson ?? (async (url: string, body: unknown) => { const response = await fetch(url, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(body) }); if (!response.ok) throw new Error(`OpenFIGI HTTP ${response.status}`); return response.json(); });
  return { sourceKey: "openfigi", priority: 10, async get(request) { const isin = request.isin.trim().toUpperCase(); const known = cache.get(isin); if (known) return known; try { const raw = await postJson("https://api.openfigi.com/v3/mapping", [{ idType: "ID_ISIN", idValue: isin }]); const first = Array.isArray(raw) ? raw[0] : null; const data = first && typeof first === "object" ? (first as { data?: unknown }).data : null; const item = Array.isArray(data) && data[0] && typeof data[0] === "object" ? data[0] as Record<string, unknown> : null; if (!item || typeof item.ticker !== "string") throw new Error("ISIN not found"); const result = { match: { isin, ticker: item.ticker, ...(typeof item.exchCode === "string" ? { exchange: item.exchCode } : {}), ...(typeof item.name === "string" ? { name: item.name } : {}) }, problems: [] }; cache.set(isin, result); return result; } catch (error) { const result = { match: { isin, ticker: "" }, problems: [`OpenFIGI identifier request failed: ${error instanceof Error ? error.message : String(error)}`] }; cache.set(isin, result); return result; } } };
}
